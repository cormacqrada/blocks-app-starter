# @blocks/cli – Blocks CLI

## Commands

### `blocks-app new backend fastapi <name>`

Create a new FastAPI backend application wired to the Blocks schemas.

- **category**: `backend`
- **template**: `fastapi`
- **name**: target directory name

The command copies the template from `templates/backend/fastapi` into the
specified directory.

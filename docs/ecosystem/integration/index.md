# Integrating Blocks into an App

1. Add `@blocks/core`, `@blocks/runtime`, and `@blocks/cli` as dependencies.
2. Adopt the schemas from `schema/` for block and collection contracts.
3. Use the CLI templates as a starting point for backend/frontend/mobile apps.

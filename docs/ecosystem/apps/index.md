# Ecosystem Apps

Each app in the Blocks ecosystem typically lives in its own repository but
shares:

- Schemas from this starter.
- Packages such as `@blocks/core`, `@blocks/runtime`, and `@blocks/cli`.
- CI, Docker, and linting patterns from `shared/`.
